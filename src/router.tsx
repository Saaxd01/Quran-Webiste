// Minimal router entry to satisfy @tanstack router generator and start plugin
// This exports getRouter which the build expects. Replace with your real router later.

export function getRouter() {
  // Minimal stub object. The build only needs the named export to exist.
  return {};
}

export default getRouter;

