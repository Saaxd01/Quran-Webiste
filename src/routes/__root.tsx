import React from "react";

// Minimal root route for TanStack Start router generator.
// Replace with your real root component and route config later.

export const route = {
  id: "__root",
  path: "/",
  children: {},
  // optional meta or loader fields can be added later
};

export default function Root() {
  return <div id="root">Loading...</div>;
}
